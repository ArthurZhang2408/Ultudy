# Python dependencies for Ultudy backend
# Install with: pip install -r requirements.txt

# PDF processing
PyMuPDF>=1.23.0  # Used for both text and vision-based extraction

# PDF to Markdown (Marker - optional alternative)
# marker-pdf>=1.10.0  # AI-powered PDF to Markdown conversion (uncomment to use)

# Vision-based extraction (optional)
google-generativeapi>=0.3.0  # Gemini Vision API
Pillow>=10.0.0  # Image processing for PDF page conversion
